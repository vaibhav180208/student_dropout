# student_dropout
Student Dropout Prediction using Decision Tree Classification predicts whether a student may drop out based on attendance, marks, study hours, and assignments. The model uses a Decision Tree Classifier, evaluates accuracy, and predicts the outcome for new students.
